public class Test {
    public static void main(String[] args) {
        String s = "/";
        int temp = Integer.parseInt(s);
        System.out.println(temp+1);
        s.length();
//         if ((s.length == 1) && (temp < 48 || temp > 57))
    }
}
